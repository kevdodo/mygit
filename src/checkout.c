#include <stdio.h>
#include <stdlib.h>
#include "checkout.h"


void checkout(const char *checkout_name, bool make_branch) {
    (void)checkout_name;
    (void)make_branch;
    printf("Not implemented.\n");
    exit(1);
}
